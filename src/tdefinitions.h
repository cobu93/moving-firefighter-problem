#include <stdint.h>

typedef uint8_t bool;

// Defined according maximum number of nodes
// In this case 255 are considered
typedef uint8_t nsize_t;  

// Defined according values in memory
// Must consider negatives (by now)
// In this case 255 are considered
typedef int msize_t; 

